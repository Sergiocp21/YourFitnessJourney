export default function PredefinedRoutinesComponent({ goBack }) {
    return (
        <div>
            <h2>Predefined Routines</h2>
            <button onClick={goBack}>Back</button>
            {/* Add more content for predefined routines here */}
        </div>
    );
}